@page
@model JobBoard.Pages.ExperienceDetails.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Experience[0].StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Experience[0].EndDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Experience[0].CompanyName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Experience[0].Position)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Experience[0].WorkDone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Experience) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkDone)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ExperienceId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ExperienceId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ExperienceId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
