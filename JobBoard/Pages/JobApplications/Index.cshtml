@page
@model JobBoard.Pages.JobApplications.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JobApplication[0].JobId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JobApplication[0].applicantId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.JobApplication) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JobId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.applicantId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.JobApplicationId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.JobApplicationId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.JobApplicationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
